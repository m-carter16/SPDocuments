export type FileCell = {
    label: string;
    type: string;
    path: string
};

export type AuthorCell = {
    label: string;
    // status: PresenceBadgeStatus;
};

export type FileIconElement = {
    Key: string;
    Value: string;
};
